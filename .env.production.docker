# ============================================
# PRODUCTION DOCKER ENVIRONMENT CONFIGURATION
# ============================================
# This file is used for Docker production deployments
# Uses internal Docker networking for backend communication

NODE_ENV=production
PUBLIC_ENVIRONMENT=production
PUBLIC_MODE=production

# ============================================
# API BASE URLS (Production Docker)
# ============================================
# IMPORTANT: Use internal Docker network URL (HTTP, not HTTPS)
# The nginx-proxy service listens on port 5000 internally
# Traefik handles SSL termination for external requests

# Internal Docker network URL (nginx-proxy container) - used for SSR requests
PUBLIC_BASE_URL=http://nginx-proxy:5000

# Public API URL for browser/client-side requests (must be publicly accessible)
# Browsers can't reach Docker internal URLs like nginx-proxy:5000
# This URL is used by axios for all client-side API calls
PUBLIC_API_URL=https://platform.getconfirmd.com

# These are kept for reference but not used in Docker
PUBLIC_BASE_URL_DEVELOPMENT=http://localhost:5000
PUBLIC_BASE_URL_STAGING=https://platform.getconfirmd.com
PUBLIC_BASE_URL_PRODUCTION=http://nginx-proxy:5000

# Frontend public URL (what users see in browser)
PUBLIC_TUNNEL_URL=https://bench.getconfirmd.com

# ============================================
# SECURITY
# ============================================
PUBLIC_CRYPTO_PRIVATE_KEY=cDw6h5FUA9gaKc7RIH4guD4AhgrBa4Q

# Keycloak Configuration
PUBLIC_KEYCLOAK_MANAGEMENT_CLIENT_ID='confirmd-live-management'
PUBLIC_KEYCLOAK_MANAGEMENT_CLIENT_SECRET=AkmTVrMsxted8CgWieTqYLhH0aCT8bMB

# ============================================
# BLOCKCHAIN CONFIGURATION
# ============================================
PUBLIC_POLYGON_TESTNET_URL='https://rpc-amoy.polygon.technology/'
PUBLIC_POLYGON_MAINNET_URL='https://polygon-rpc.com/'

# ============================================
# PLATFORM BRANDING
# ============================================
PUBLIC_SHOW_NAME_AS_LOGO=true
PUBLIC_PLATFORM_NAME=WorkBench
PUBLIC_PLATFORM_LOGO=/images/confirmd1-logo.png
PUBLIC_POWERED_BY=Credence Networks Inc.
PUBLIC_PLATFORM_WEB_URL=https://platform.getconfirmd.com/
PUBLIC_PLATFORM_DOCS_URL=https://credence.com.ng/
PUBLIC_PLATFORM_GIT=https://github.com/credenceng
PUBLIC_PLATFORM_TWITTER_URL="https://twitter.com/i/flow/login?redirect_after_login=%2Fcrdnc"
PUBLIC_PLATFORM_DISCORD_SUPPORT="https://discord.gg/credence"

# ============================================
# CONTENT SECURITY POLICY
# ============================================
# Note: Browser requests still use HTTPS URLs
# Internal Docker requests use HTTP but browsers don't see those
PUBLIC_ALLOW_DOMAIN="https://bench.getconfirmd.com https://platform.getconfirmd.com https://cdnjs.cloudflare.com https://cdn.jsdelivr.net https://tailwindcss.com https://fonts.googleapis.com https://fonts.gstatic.com https://avatars.githubusercontent.com https://flowbite-admin-dashboard.vercel.app/ https://rpc-amoy.polygon.technology/ wss://platform.getconfirmd.com https://minio.getconfirmd.com/ https://agent.getconfirmd.com"

# ============================================
# LOGGING CONFIGURATION
# ============================================
LOG_LEVEL=info
LOG_FORMAT=json
LOG_TO_FILE=true
LOG_DIR=logs
